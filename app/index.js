import { Redirect } from "expo-router";
import { SignedOut, SignedIn } from "@clerk/clerk-react";
import { Text, View } from "react-native";

const Index = () => {
  return <View style={{ flex: 1 }} className="bg-[#3D44DB] h-screen"></View>;
};

export default Index;
